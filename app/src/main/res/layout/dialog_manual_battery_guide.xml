<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/dialog_background_m3"
    android:padding="24dp">

    <!-- 标题 -->
    <TextView
        android:id="@+id/tv_dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="手动设置电池优化"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnSurface"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="8dp" />

    <!-- 内容 -->
    <TextView
        android:id="@+id/tv_dialog_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="请根据您的手机品牌手动设置：\n\n📱 小米/红米：\n设置 → 电池与性能 → 应用电量管理 → 找到本应用 → 无限制\n\n📱 华为/荣耀：\n设置 → 电池 → 启动管理 → 关闭本应用自动管理 → 允许后台活动\n\n📱 三星S23 (One UI 6.0+)：\n设置 → 应用程序 → 找到本应用 → 后台控制 → 关闭优化\n\n📱 三星其他机型：\n设置 → 应用程序 → 本应用 → 电池 → 允许后台活动\n\n📱 OPPO/一加：\n设置 → 电池 → 应用耗电管理 → 找到本应用 → 不限制\n\n📱 vivo：\n设置 → 电池 → 后台耗电管理 → 找到本应用 → 允许后台高耗电\n\n设置完成后，大文件下载将不会被系统中断。"
        android:textSize="16sp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="24dp" />

    <!-- 按钮容器 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <!-- 取消按钮 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_cancel"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="取消"
            android:textColor="?attr/colorPrimary"
            android:layout_marginEnd="8dp" />

        <!-- 我知道了按钮 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_confirm"
            style="@style/Widget.Material3.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="我知道了"
            app:backgroundTint="?attr/colorPrimary"
            android:textColor="?attr/colorOnPrimary" />

    </LinearLayout>

</LinearLayout>
